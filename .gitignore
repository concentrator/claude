# Harness-managed state
history.jsonl
session-env/
projects/
file-history/
cache/
paste-cache/
shell-snapshots/
backups/
sessions/
tasks/
telemetry/
downloads/
debug/
chrome/
usage-data/

# Harness-managed cache/state files
skill_invocations.jsonl
mcp-needs-auth-cache.json
policy-limits.json
remote-settings.json
stats-cache.json
.last-cleanup
.last-update-result.json

# Local overrides
*.local.json
*.local.md

# Project tier: everything machine-local except the tracked settings
.claude/*
!.claude/settings.json

# Plugins (marketplace-managed)
plugins/

# Project-specific skills, symlinked from external repos (versioned there)
skills/wallarm-*

# DEV session state - hooks/dev-precompact-state.sh, skills/dev/handoff.md
/dev/session/

# OS
.DS_Store
supervisor/

# Secrets - baseline requirement (layout.md § Baseline files)
.env
.env.*
!.env.example

# Python bytecode from importing scripts/*.py
__pycache__/

# Claude Code credentials and local state (worker hosts make this dir a repo)
.credentials.json
downloads/
