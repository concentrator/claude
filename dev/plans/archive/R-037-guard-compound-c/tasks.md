# R-037 tasks

Tasks for R-037 (`plans/R-037-guard-compound-c/requirements.md`) - the
branch-create exemption tolerates git global options. Format per
`skills/dev/plan.md § Levels`.

- [x] T-077 (R-037) [fix]: accept global options between `git` and `checkout`/`switch` in the branch-create exemption - mirror the commit-regex option group, shift the capture index; pins for the exempted and adversarial `-C` shapes in the same slice
