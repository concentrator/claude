# R060-T001 findings

- [x] Promoted to a backlog line in `tasks.md`. `skills/dev/plan.md` sits at the 1500-word reference cap
      (`scripts/ci/check-caps.sh`) after § Milestone plans; the section
      fit only by removing text the file stated twice. The next rule
      added to `plan.md` needs a companion file or a further split.
