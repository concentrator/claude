# R066: Slim the DEV skill - tasks

- [x] R066-T001 [refactor]: remove the visual companion
  (`visual-companion.md`, `companions/scripts/`, the `brainstorm.md`
  offer, the `layout.md` entry, the `code-size-allow.txt` line) and
  return the `supervisor-runbook.md` measurements to their R-040
  findings or `skills/worker-host`; installer self-test and `DESIGN.md`
  tree-map follow
- [x] R066-T002 [refactor]: one home per rule (the eight-row table),
  stale text, initiative attributions, the nine rationale trims,
  `secrets.md` re-homed as `companions/secrets.md` with its pattern
  list citing the hook, `plan.md` citing `layout.md`; the branch plan
  carries the rule-to-home table
- Backlog: `companions/supervisor-runbook.md` cites `skills/worker-host/`
  paths the installer does not ship, so the citation is dead in an
  adopter tree; same both-trees concern as T002's `scripts/ci/` row
  (surfaced by the R066-T001 close review).
