# R077: Ship the maintenance routine - tasks

- [x] R077-T001 [feat]: seed the hygiene section on `--project`
  installs - the canonical section shipped with the toolset, appended
  to the target's `.claude/MAINTENANCE.md` only when its heading is
  absent (file created when missing), the global path untouched,
  `install-dev.test.sh` cases for each seeding path and the
  reworded `MAINTENANCE.md` claim
