# R075: Installer delivery guard - tasks

- [ ] R075-T001 [feat]: the two pre-write refusals in
  `install-dev.sh` - dirty tracked tree, default-branch HEAD - with
  `--force` bypassing both, stderr messages naming the remedy, and
  `install-dev.test.sh` cases for each refusal, the override, and the
  unchanged clean/non-git/global paths
