# R-042 tasks - Planning-round PoCs

This initiative's task index. The tag sets the branch prefix; a
checkbox closes only when the task's branch merges. Task ids are
composite (`R042-T###`, counter scoped to this initiative).

## Open

- [ ] **R042-T001 [doc]**: the spike provision - `plan.md` (rounds)
  gains the when-to-spike rule, time box, findings home, and discard
  rule; `write-plan.md` step 3 extends the probe rule so a plan cites
  spike findings exactly as probe findings.
