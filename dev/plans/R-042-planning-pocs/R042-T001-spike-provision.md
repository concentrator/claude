---
task: R042-T001
type: doc
agentic: approved 2026-08-09
---

# R042-T001 - the spike provision

Branch: `doc/spike-provision`.

- [ ] `plan.md § Planning rounds`: the spike provision - a shape or
      detail round MAY run a throwaway spike in a worktree when a
      task's approach carries an unproven assumption. Time box:
      declared when the spike is proposed, default one hour; exceeding
      it stops the spike and reports. Findings land where probe
      findings live - shape round: the R's requirements; detail round:
      the branch plan's citations. Spike code is always discarded,
      never merged, never cited as implementation (cap-aware: plan.md
      stays within 1500 words).
- [ ] `write-plan.md` step 3: the probe rule accepts spike findings as
      citable ground exactly like probe findings ("probe first, then
      plan" gains "or spike").
- [ ] Complete the branch: re-review docs across all commits, cleanup
      (stale/temp data), mark plan complete, commit.
