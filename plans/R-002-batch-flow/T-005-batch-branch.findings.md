- [x] `delegating-to-agents/SKILL.md:46` ("merge to **local** default
  branch") contradicts the new batch-branch rails until T-006 rewrites
  the engine - known seam; do not run `/dev auto` anywhere before
  T-006 merges. (Discarded as already-covered: engine rewrite is
  T-006's existing first-line scope, depends-on-chained.)
