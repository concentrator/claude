# R047-T003 findings

- [x] The harness `/code-review` skill - the manual close's prose
  reviewer - is not repo-tracked, so it cannot carry the changed-claims
  mandate itself. Won't fix: out of repo scope. Manual-path coverage is
  the closing routine's step 7, which completes every shipped doc
  through the verification gate, whose text states the check.
