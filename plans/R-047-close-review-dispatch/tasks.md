# R-047 tasks - Branch-close routing

This initiative's task index. The tag sets the branch prefix; a
checkbox closes only when the task's branch merges. Task ids are
composite (`R047-T###`, counter scoped to this initiative).

## Open

- [x] **R047-T001 [doc]**: review routing - the content-keyed dispatch
  table into `branch-plan.md § Closing routine` step 1 (size governor
  and Tier-2 line untouched, cap held by trading words), the
  verification gate stating what clears it per prose class in
  `documentation.md`, and `delegation.md`'s fan-out clause citing the
  content rule. Walked against R-046's five branches plus this one.
- [x] **R047-T002 [doc]**: close-mechanics seams - `finish.md § 2`
  names a verify action per diff content (code, prose, test, data),
  and `toolchain.md`'s push-scoping header names whom the batch-accept
  restriction binds so it stops contradicting `finish.md § 3`.
- [ ] **R047-T003 [doc]**: prose-review mandate - the verification
  gate's rules-prose path names review behavior a repo-tracked reviewer
  definition specifies: checking the changed claims against their
  sources (`agents/code-reviewer.md` or the closing routine's step
  text). Promoted from R047-T001's close review.
