# R-047 tasks - Close-review dispatch for every task tag

This initiative's task index. The tag sets the branch prefix; a
checkbox closes only when the task's branch merges. Task ids are
composite (`R047-T###`, counter scoped to this initiative).

## Open

- [ ] **R047-T001 [doc]**: content-keyed close-review dispatch -
  `branch-plan.md § Closing routine` step 1 becomes the content table,
  the size governor and Tier-2 line left untouched, the table walked
  against R-046's five branches plus this one, and the cap held by
  trading words rather than adding them.
