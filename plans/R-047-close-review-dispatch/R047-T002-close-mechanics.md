---
task: R047-T002
type: doc
---

# R047-T002 - close-mechanics seams

Branch: `doc/close-mechanics`.

- [ ] `finish.md § 2`: the verify step names an action per diff
      content - code → run it; rules or process prose → dry-run the
      changed rule against a real case; tests → the suite run is the
      verification; data → run the work product and show the results
      (unchanged today).
- [ ] `toolchain.md`'s header: the batch-accept push restriction names
      whom it binds - the auto-mode engine - so `finish.md § 3`'s
      manual branch push stops contradicting it; both sentences true at
      once.
- [ ] Mark and commit the task `[x]` in the R's `tasks.md`.
- [ ] Complete the branch: re-review docs across all commits, cleanup
      (stale/temp data), mark plan complete, commit.
