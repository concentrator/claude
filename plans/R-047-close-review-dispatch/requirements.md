---
approved: pending
kind: doc
---

# R-047: Close-review dispatch for every task tag

Stub - to be shaped (`/dev plan R`). Draft backlog, carried over at
R-046's close:

- `branch-plan.md § Closing routine` step 1 dispatches the close review
  by `refactor` / `feature` / `bugfix` only, so a `doc`, `test`, or
  `mnt` branch has no declared route. R-046 closed the analogous gap for
  the execution dispatch (`SKILL.md § /dev code` now routes all six
  tags); the close-review one stayed open, and every `doc` and `test`
  branch in R-046 had to ask the user which review to run.
