task: R052-T003
type: doc

# R052-T003 - Branch before the first artifact write

- [ ] State the sequence in the shape and detail flows - the plan
  branch exists before the first artifact write: `brainstorm.md`
  (§ Process, draft step) and `write-plan.md` (step 6)
- [ ] The same statement where the remaining planning writers act:
  batch composition (`branch-plan.md § Batches`, the `/dev plan
  batch` path) and the supervisor portfolio (`supervise.md`)
- [ ] Mark and commit the task `[x]` in the R's `tasks.md`, plus any
  release-plan entry
- [ ] Complete the branch: re-review docs across all commits, cleanup
  (stale/temp data), mark plan complete, commit
