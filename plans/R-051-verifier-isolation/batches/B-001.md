# B-001

Mode: manual. Members carry no `agentic:` stamp, so `/dev code` runs each
and `finish.md` closes it.

- R051-T001 (verifier-isolation)
- R051-T003 (revendor-tests)

T001 fixes this repo; T003 fixes the copies `install-dev.sh` has already
shipped to adopters. Landing either alone leaves a half-true claim on
`main` - the defect fixed here while adopters still hold the unsafe
tests - so the two deliver as one PR.

T002 is not a member. It adds a regression guard and leaves `main`
coherent on its own, so it fails the coupling test (§ Batches), and its
own plan leaves its approach open. Batching it would hold the fix behind
an unsettled design. It runs as a task of one once this batch merges.
