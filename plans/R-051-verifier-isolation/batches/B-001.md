# B-001

Mode: manual. Members carry no `agentic:` stamp, so `/dev code` runs each
and `finish.md` closes it.

Order is not id order. T002 and T003 both depend on T001 alone, and T002
is the member whose approach its own plan leaves open, so it runs last:
a halt there leaves the fix and the re-vendor already merged into the
batch branch, where the manifest can be recomposed around them.

- R051-T001 (verifier-isolation)
- R051-T003 (revendor-tests)
- R051-T002 (host-untouched)
