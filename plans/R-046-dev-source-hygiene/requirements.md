---
approved: pending
kind: mnt
---

# R-046: DEV system-source hygiene

Stub - to be shaped (`/dev plan R`). Draft backlog, carried over at
R-045's close:

- `companions/toolchain.md` hosts three `CLAUDE.md § Agent toolchain`
  declaration keys (commands, supervisor bounds, artifacts root) in a
  file titled for push/MR mechanics; a companion owning declaration
  syntax as such would leave `toolchain.md` the push mechanics.
- `scripts/ci/check-plan-integrity.sh` lacks a dedicated script test;
  its root-seam behavior (nested-root attribution, the missing-ROADMAP
  guard) is covered only by the accretion suite's resolver cases.
