# R045-T001 findings

- [x] `companions/toolchain.md` now documents three `CLAUDE.md § Agent
  toolchain` keys (commands, supervisor bounds, artifacts root) in a
  file titled for push/MR mechanics; consider a companion owning the
  declaration syntax as such, leaving toolchain.md the push
  mechanics. (Close review, altitude angle.) Routed: backlog line in
  the R-045 `tasks.md`.
