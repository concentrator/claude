# B-001 report — verification-depth policy, close folding, slim dispatch (R-005 levers 1–4)

batch-branch: batch/B-001
base: main@c504eac
state: 3/3 branches merged, caps green (SKILL.md 400/400), sweeps green

## Branches

### feat/verif-depth (T-012)
- commits: 7 (5371e73..1edc523, incl. final)
- tests: no suite — green = `wc -w` caps + grep sweeps, all passing
- spec checks: 6 passed, 0 rejected→fixed, 0 skipped (policy landed with
  this branch; old routine applied)
- review findings: 2 mechanical (stale intra-branch ref; bracket glitch)
  fixed in 1edc523 | 3 judgment calls queued (see below)
- findings file: none

### feat/close-folding (T-013)
- commits: 9 (21c0424..6c41d17, incl. controller fixup + final)
- tests: caps + sweeps green
- spec checks: 0 passed, 0 rejected, **6 skipped: mechanical**
  (21c0424, e8e3565, 5081261, fad1f96, f0c286e, 71eb232)
- review findings: 1 important (folded-list mechanism implicit) fixed in
  6c41d17 | controller-caught defect on a skipped commit: f0c286e
  stated the folding rule inverted and named the wrong review — fixed
  in 66d1aa9 before close | wording suggestions queued
- findings file: none

### refactor/slim-dispatch (T-014)
- commits: 7 (d349b1f..e65310e, incl. corrective + final)
- tests: caps + sweeps green
- spec checks: 0 passed, 0 rejected, **5 skipped: mechanical**
  (d349b1f, 35a46d0, 77a0909, b8f07ed, 35eaee2)
- review findings: 1 important (drift-only outcome unexpressible in
  binary report) fixed in e65310e | controller-caught defect on a
  skipped commit: d349b1f bloated the prompt 866→941 against the
  lever's intent — corrected to 880 in f057587 | judgment calls queued
- findings file: none

## Batch review (full diff vs base, most capable model)

- cross-branch findings: DESIGN.md tree-map missed the new
  verification-policy.md (fixed c72b257); drift-outcome handling lived
  only in the reviewer prompt — controller-side semantics added to the
  policy (fixed c72b257); lever-1×lever-4 interaction (drift sensor
  blind on skipped commits) and judgment-heavy routing predicate —
  queued as judgment calls; SKILL.md three-branch interleave verified
  coherent at exactly 400/400.
- fixes applied on batch branch: c72b257
- tests + lint after fixes: caps + sweeps green

## Docs coherence

- CHANGELOG: n/a (repo has none)
- README / extended docs: consistent; DESIGN.md tree-map updated on the
  batch branch (judgment call: lands via MR, not direct to main)

## Cost

- total subagent tokens: ~794k (implementer: ~440k / spec-check: ~128k /
  reviews: ~226k — three branch closes + batch review)
  B-002/B-003 baseline: ~800–900k subagent tokens / 12 commits
  **per-commit: ~40k vs ~70k baseline (−43%)** across 20 landed commits
- spec checks skipped: 11 (records above, per
  verification-policy.md § Spec-check skip)
- dispatch-prompt sizes (wc -w): implementer-prompt.md 866 → 804;
  spec-reviewer-prompt.md 312 → 390
  (lever-4 baseline, pre-T-014: implementer 866, spec-reviewer 312;
  spec-reviewer growth is the deliberate drift sensor — net economics
  hold because mechanical commits skip that dispatch entirely)
- convention drift flagged by spec checks: 0 (B-002/B-003 baseline: 0;
  sensor landed mid-batch — first full reading next batch)
- defect outcomes: spec rejections reaching merge: 0
  (B-002/B-003 baseline: 0 merge-reaching spec rejections);
  review findings surfaced: 4 fixed / 8 queued / 0 dismissed;
  **2 defects on spec-check-skipped commits caught by controller
  mid-branch (inverted rule f0c286e→66d1aa9; prompt bloat
  d349b1f→f057587) — the skip's fallback nets worked, but both would
  have been caught earlier by spec checks**

## R acceptance criteria

R-005 (this batch's parent):
- AC1 (verification-depth policy defined): verified — policy file,
  routing table, folding entry shipped (T-012/T-013).
- AC2 (mechanical skip by stated checkable rule): verified — predicate
  exercised 11 times this run, guard checked per commit.
- AC3 (batch reports cost + defects vs baseline): verified — this
  report's Cost section.
- AC4 (no increase in defects reaching MR): verified — 0 vs baseline 0.
- AC5 (per-dispatch baseline reduced, planning rules path-scoped):
  still pending — T-015 (context diet), manual, not in this batch.
  R-005 stays open.

R-003 (awaiting run-dependent validation — re-triggered by this
checkpoint per the closure rule):
- "Batch checkpoint re-triggers verification": verified — this section.
- "Batch-close marks checkboxes on the batch branch, MR carries closed
  state": verified — manifest + TASKS.md marks committed on
  batch/B-001 below, no post-merge closure commit planned.
- "New initiative end-to-end without path/routing errors": verified —
  R-005 ran create→approve→tasks→plans→batch with zero path or routing
  errors. Formal R-003 closure stamp runs post-merge per the rule.

## Prompt friction

log empty — zero permission prompts during the run.

## Judgment calls for checkpoint

1. **Guard vs convention-mandated doc files** (T-012 review): projects
   with per-commit CHANGELOG conventions void every mechanical skip.
   Options: exempt convention-mandated doc files from the guard/count
   (recommended) | require plans to name doc files | keep as-is.
2. **Spec checks on sonnet** vs REQ's literal "all reviews → Fable":
   applied sonnet (cheaper, preserves old "fast" tier) — confirm or
   flip to fable.
3. **Skip-record wording** ("running batch log" → records carried into
   report) applied per reviewer recommendation — confirm.
4. **Drift sensor blind on skipped commits** (lever-1×4 interaction):
   accept with caveat noted in template (recommended) | widen Cost
   line to count close-review drift findings too.
5. **"Judgment-heavy implementers" has no predicate**: state that
   auto-mode items default to the Default row unless the plan marks
   them (recommended) | define a predicate.
6. **SKILL.md at exactly 400/400** — zero headroom; next edit forces
   trims. Accept, or commission a structural slim in a future task.
7. **Two controller catches on skipped commits** — both fixed, but
   worth noting: lever 1's safety relied on controller attention this
   run. The branch-close/batch reviews are the systematic net; they
   also caught independent issues. No action proposed; data point for
   the next batch's comparison.
